{
  "name": "react-insight-ai",
  "version": "1.0.0",
  "description": "A Chrome DevTools extension that hooks into a webpage's React Fiber tree and visualizes it",
  "scripts": {
    "build": "tsc && npm run copy-assets",
    "copy-assets": "cp popup.html dist/ && cp styles.css dist/ && cp hashira.png dist/",
    "watch": "tsc --watch",
    "clean": "rimraf dist",
    "dev": "npm run clean && npm run build && npm run watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "typescript": "^5.3.0",
    "rimraf": "^5.0.0"
  },
  "private": true
}